name: Sketch

on:
  push:
    branches: [main]
    paths:
      - "sketch/**"
      - ".github/workflows/sketch.yml"
  pull_request:
    branches: [main]
    paths:
      - "sketch/**"
      - ".github/workflows/sketch.yml"

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v4
        with:
          working_directory: sketch
      - name: Run clang-format
        run: mise lint
        working-directory: sketch

  compile:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        profile: [default, minima]
    steps:
      - uses: actions/checkout@v6

      - name: Cache Arduino Dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.arduino15
            ~/Arduino
          key: arduino-${{ matrix.profile }}-${{ hashFiles('sketch/**') }}
          restore-keys: |
            arduino-${{ matrix.profile }}-
            arduino-

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Install Platforms
        run: |
          arduino-cli core update-index
          arduino-cli core install arduino:zephyr arduino:renesas_uno

      - name: Compile Sketch (${{ matrix.profile }})
        run: arduino-cli compile -m ${{ matrix.profile }} .
        working-directory: sketch
